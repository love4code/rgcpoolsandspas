<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %><%= settings && settings.companyName ? settings.companyName : 'RGC Pool and Spa' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <%- include('../partials/theme-styles', { settings: typeof settings !== 'undefined' ? settings : null }) %>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .hero-section {
      min-height: 60vh;
      display: flex;
      align-items: center;
    }
    .service-card {
      transition: transform 0.3s;
      height: 100%;
    }
    .service-card:hover {
      transform: translateY(-5px);
    }
    .portfolio-card {
      overflow: hidden;
      transition: transform 0.3s;
    }
    .portfolio-card:hover {
      transform: scale(1.05);
    }
    .contact-form {
      background: #f8f9fa;
      padding: 2rem;
      border-radius: 10px;
    }
  </style>
</head>
<body>
  <%- include('../partials/public-header', { settings: typeof settings !== 'undefined' ? settings : null }) %>
  
  <main>
    <%- body %>
  </main>

  <%- include('../partials/public-footer', { settings: typeof settings !== 'undefined' ? settings : null }) %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

